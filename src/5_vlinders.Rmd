# Vlinders

```{r}
sg <- "dagvlinders"
```

## Overzicht monitoringsinspanning

```{r}

overzicht_bezoeken_tellers %>%
  filter(soortgroep == sg) %>%
  select(-soortgroep) %>%
  kable() %>%
  kable_styling("bordered", bootstrap_options = c("hover", "condensed")) %>%
  collapse_rows(1) %>%
  scroll_box(width = "100%", height = "600px")

```

## Overzicht getelde aantallen {.tabset}

### Primaire soorten

```{r}

overzicht_aantallen_levensstadium_primair_wide %>%
  filter(soortgroep == sg) %>%
  select(-soortgroep, -soort_nl, "Nederlandse naam" = meetnet, "wetenschappelijke naam" = soort_wet) %>%
  kable() %>%
  kable_styling(bootstrap_options = c("hover", "condensed")) %>%
    column_spec(2, italic = TRUE) %>%
    collapse_rows(c(1,2,3), target = 1) %>%
  scroll_box(width = "100%", height = "600px")
  
```

### Overige soorten

```{r}
overzicht_aantallen_levensstadium_overige_wide %>%
  mutate(soort_nl = ifelse(soort_wet == "Pelophylax esculenta synklepton",
                           "groene-kikker-complex", soort_nl)) %>%
  filter(soortgroep == sg) %>%
  select(-soortgroep, "Nederlandse naam" = soort_nl, "wetenschappelijke naam" = soort_wet) %>%
  kable() %>%
  kable_styling("bordered", bootstrap_options = c("hover", "condensed")) %>%
    column_spec(2, italic = TRUE) %>%
    collapse_rows(c(1,2,3), target = 1) %>%
  scroll_box(width = "100%", height = "600px")
```

## Verkenning getelde aantallen {.tabset}

### Aardbeivlinder

```{r}
soort <- "Aardbeivlinder"

```


```{r Aardbeivlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Aardbeivlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal, 
 #            size = aantalSectiesGeteld, 
             colour = locatie)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
 
```





### Argusvlinder

```{r}
soort <- "Argusvlinder"

```

```{r Argusvlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 4}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Argusvlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 8, fig.height= 3}

max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
 
```







### Bruin dikkopje

```{r}
soort <- "Bruin dikkopje"

```

```{r Bruindikkopjegemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 4}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Bruindikkopje, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}

max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
 
```







### Bruine eikenpage

```{r}
soort <- "Bruine eikenpage"

```

```{r Bruineeikenpagegemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Bruineeikenpage, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 5, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
  
```






### Gentiaanblauwtje

```{r}
soort <- "Gentiaanblauwtje"

```

```{r Gentiaanblauwtjegem, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 4}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Gentiaanblauwtje, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 
```







### Grote weerschijnvlinder

```{r}
soort <- "Grote weerschijnvlinder"

```


```{r Groteweerschijnvlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   filter(doy < 200) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Groteweerschijnvlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}

max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

```





### Heivlinder

```{r}
soort <- "Heivlinder"

```


```{r Heivlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}
aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Heivlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 8, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
 
```





### Klaverblauwtje

```{r}
soort <- "Klaverblauwtje"

```


```{r Klaverblauwtjegemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Klaverblauwtje, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie, 
             colour = locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 
```




### Kommavlinder

```{r}
soort <- "Kommavlinder"

```

```{r Kommavlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 4}
aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Kommavlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 8, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

 q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
 
```



### Moerasparelmoervlinder

```{r}
soort <- "Moerasparelmoervlinder"

```

```{r Moerasparelmoervlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}
aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Moerasparelmoervlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 3, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie, 
             colour = locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 


 
```



### Oranje zandoogje

```{r}
soort <- "Oranje zandoogje"

```

```{r Oranjezandoogjegemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}
aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))

aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Oranjezandoogje, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 6, fig.height= 3}



max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

  q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
```






### Veldparelmoervlinder

```{r}
soort <- "Veldparelmoervlinder"

```

```{r Veldparelmoervlindergemiddeld, fig.cap= str_c("Evolutie gemiddelde getelde aantallen per bezoek: ", soort), fig.height= 3, fig.width= 3}

aantallen_soort <- aantallen_bezoek_totaal %>%
   filter(primaire_soort) %>%
   filter(meetnet == soort ) %>%
   mutate(doy_date = as.Date(doy, origin = "2016-01-01"))


aantallen_soort %>%
  ggplot(aes(x= jaar, y = aantal)) +  
  #geom_point(alpha = 0.6, colour = inbo.grijs) +
  stat_summary(fun.data = "mean_cl_boot", colour = inbo_rood, size = 1, alpha = 0.8) +
  facet_wrap(~ soort_nl, scale = "free_y") +
  labs(y = "Aantal getelde individuen", x = "Jaar") +
  theme(legend.position = "bottom") 

```

```{r Veldparelmoervlinder, fig.cap= str_c("Evolutie getelde individuen binnen telperiode: ", soort) , message=FALSE, warning= FALSE, fig.width= 8, fig.height= 3}


max_soort <- max(aantallen_soort$aantal) * 1.2

 p <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal,
             group= locatie)) + 
  geom_point(alpha = 0.6) +  
 geom_line(linetype = 2,  alpha = 0.6, size = 0.4, colour = inbo_grijsblauw)  + 
  facet_grid(soort_nl ~ jaar) +
  labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90))
  p #ggplotly(p) 

  q <- aantallen_soort %>%
  ggplot(aes(x = doy_date, 
             y = aantal)) + 
  geom_point(alpha = 0.6) + 
  geom_smooth(method = "gam", method.args = list(family = poisson),formula = y ~ s(x, bs = "cs", k = 6) , size = 0.4, colour = "blue") +
  facet_grid(soort_nl ~ jaar) +
  scale_x_date(date_breaks = "1 month", date_labels = "%b") +
   labs(x = "Datum bezoek", y = "Aantal getelde individuen") + 
  ylim(0, max_soort) + 
  theme(legend.position = 'none', axis.text.x = element_text(angle = 90)) 
 q #ggplotly(q)
```



